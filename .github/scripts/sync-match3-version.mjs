import fs from 'node:fs';
import path from 'node:path';

const rawVersion = process.argv[2];

if(!rawVersion){
    console.error('Missing semantic version argument.');
    process.exit(1);
}

const match = rawVersion.match(/^(\d+)\.(\d+)\.(\d+)(?:-([0-9A-Za-z.-]+))?$/);
if(!match){
    console.error(`Invalid semantic version: ${rawVersion}`);
    process.exit(1);
}

const [, major, minor, patch, prerelease = ''] = match;
const buildDate = new Date().toISOString();

// ── Root version.js ──
const versionTarget = path.resolve(process.cwd(), 'version.js');
const nextContent = `export const SITE_SEMVER = {
  major: ${Number(major)},
  minor: ${Number(minor)},
  patch: ${Number(patch)},
  prerelease: '${prerelease}',
  buildDate: '${buildDate}',
};

export function getVersion() {
  const { major, minor, patch, prerelease } = SITE_SEMVER;
  const base = \`${'${major}'}.${'${minor}'}.${'${patch}'}\`;
  return prerelease ? \`${'${base}'}-${'${prerelease}'}\` : base;
}

export function getBuildDate() {
  return SITE_SEMVER.buildDate || '';
}
`;
fs.writeFileSync(versionTarget, nextContent, 'utf8');
console.log(`Updated ${versionTarget} -> ${rawVersion}`);

// ── times-up/version.js ──
const timesUpVersionTarget = path.resolve(process.cwd(), 'times-up/version.js');
const timesUpVersionContent = `// This file is auto-generated by .github/scripts/sync-match3-version.mjs
// It mirrors root /version.js so that the times-up SW module can import it.
export const SITE_SEMVER = {
  major: ${Number(major)},
  minor: ${Number(minor)},
  patch: ${Number(patch)},
  prerelease: '${prerelease}',
  buildDate: '${buildDate}',
};

export function getVersion() {
  const { major, minor, patch, prerelease } = SITE_SEMVER;
  const base = \`${'${major}'}.${'${minor}'}.${'${patch}'}\`;
  return prerelease ? \`${'${base}'}-${'${prerelease}'}\` : base;
}

export function getBuildDate() {
  return SITE_SEMVER.buildDate || '';
}
`;
fs.writeFileSync(timesUpVersionTarget, timesUpVersionContent, 'utf8');
console.log(`Updated ${timesUpVersionTarget} -> ${rawVersion}`);

// ── flash-guess/version.js (Capacitor bundle mirror of root version.js) ──
const flashGuessVersionTarget = path.resolve(process.cwd(), 'flash-guess/version.js');
const flashGuessVersionContent = `// This file is auto-generated by .github/scripts/sync-match3-version.mjs
// It mirrors root /version.js so that Capacitor (webDir: flash-guess) can bundle it.
export const SITE_SEMVER = {
  major: ${Number(major)},
  minor: ${Number(minor)},
  patch: ${Number(patch)},
  prerelease: '${prerelease}',
  buildDate: '${buildDate}',
};

export function getVersion() {
  const { major, minor, patch, prerelease } = SITE_SEMVER;
  const base = \`${'${major}'}.${'${minor}'}.${'${patch}'}\`;
  return prerelease ? \`${'${base}'}-${'${prerelease}'}\` : base;
}

export function getBuildDate() {
  return SITE_SEMVER.buildDate || '';
}
`;
fs.writeFileSync(flashGuessVersionTarget, flashGuessVersionContent, 'utf8');
console.log(`Updated ${flashGuessVersionTarget} -> ${rawVersion}`);

// ── flash-guess/sw.js — CACHE version ──
const fgSwTarget = path.resolve(process.cwd(), 'flash-guess/sw.js');
let fgSwContent = fs.readFileSync(fgSwTarget, 'utf8');
fgSwContent = fgSwContent.replace(
  /^const CACHE = 'flashguess-v[^']+';$/m,
  `const CACHE = 'flashguess-v${Number(major)}.${Number(minor)}.${Number(patch)}';`
);
fs.writeFileSync(fgSwTarget, fgSwContent, 'utf8');
console.log(`Updated flash-guess/sw.js CACHE -> flashguess-v${rawVersion}`);

// ── times-up/sw.js — CACHE version ──
const tuSwTarget = path.resolve(process.cwd(), 'times-up/sw.js');
let tuSwContent = fs.readFileSync(tuSwTarget, 'utf8');
tuSwContent = tuSwContent.replace(
  /^const CACHE = 'timesup-v[^']+';$/m,
  `const CACHE = 'timesup-v${Number(major)}.${Number(minor)}.${Number(patch)}';`
);
fs.writeFileSync(tuSwTarget, tuSwContent, 'utf8');
console.log(`Updated times-up/sw.js CACHE -> timesup-v${rawVersion}`);

// ── game-template/version.js ──
const gameTemplateVersionTarget = path.resolve(process.cwd(), 'game-template/version.js');
const gameTemplateVersionContent = `// This file is auto-generated by .github/scripts/sync-match3-version.mjs
// It mirrors root /version.js.
export const SITE_SEMVER = {
  major: ${Number(major)},
  minor: ${Number(minor)},
  patch: ${Number(patch)},
  prerelease: '${prerelease}',
  buildDate: '${buildDate}',
};

export function getVersion() {
  const { major, minor, patch, prerelease } = SITE_SEMVER;
  const base = \`${'${major}'}.${'${minor}'}.${'${patch}'}\`;
  return prerelease ? \`${'${base}'}-${'${prerelease}'}\` : base;
}

export function getBuildDate() {
  return SITE_SEMVER.buildDate || '';
}
`;
fs.writeFileSync(gameTemplateVersionTarget, gameTemplateVersionContent, 'utf8');
console.log(`Updated ${gameTemplateVersionTarget} -> ${rawVersion}`);

// ── game-template/sw.js — CACHE version ──
const gtSwTarget = path.resolve(process.cwd(), 'game-template/sw.js');
let gtSwContent = fs.readFileSync(gtSwTarget, 'utf8');
gtSwContent = gtSwContent.replace(
  /^const CACHE = 'template-v[^']+';$/m,
  `const CACHE = 'template-v${Number(major)}.${Number(minor)}.${Number(patch)}';`
);
fs.writeFileSync(gtSwTarget, gtSwContent, 'utf8');
console.log(`Updated game-template/sw.js CACHE -> template-v${rawVersion}`);

// ── pyramide/version.js ──
const pyramideVersionTarget = path.resolve(process.cwd(), 'pyramide/version.js');
const pyramideVersionContent = `// This file is auto-generated by .github/scripts/sync-match3-version.mjs
// It mirrors root /version.js.
export const SITE_SEMVER = {
  major: ${Number(major)},
  minor: ${Number(minor)},
  patch: ${Number(patch)},
  prerelease: '${prerelease}',
  buildDate: '${buildDate}',
};

export function getVersion() {
  const { major, minor, patch, prerelease } = SITE_SEMVER;
  const base = \`${'${major}'}.${'${minor}'}.${'${patch}'}\`;
  return prerelease ? \`${'${base}'}-${'${prerelease}'}\` : base;
}

export function getBuildDate() {
  return SITE_SEMVER.buildDate || '';
}
`;
fs.writeFileSync(pyramideVersionTarget, pyramideVersionContent, 'utf8');
console.log(`Updated ${pyramideVersionTarget} -> ${rawVersion}`);

// ── pyramide/sw.js — CACHE version ──
const pyrSwTarget = path.resolve(process.cwd(), 'pyramide/sw.js');
let pyrSwContent = fs.readFileSync(pyrSwTarget, 'utf8');
pyrSwContent = pyrSwContent.replace(
  /^const CACHE = 'pyramide-v[^']+';$/m,
  `const CACHE = 'pyramide-v${Number(major)}.${Number(minor)}.${Number(patch)}';`
);
fs.writeFileSync(pyrSwTarget, pyrSwContent, 'utf8');
console.log(`Updated pyramide/sw.js CACHE -> pyramide-v${rawVersion}`);

// ── dj-mix/version.js (Capacitor bundle mirror of root version.js) ──
const djMixVersionTarget = path.resolve(process.cwd(), 'dj-mix/version.js');
const djMixVersionContent = `// This file is auto-generated by .github/scripts/sync-match3-version.mjs
// It mirrors root /version.js so that Capacitor (webDir: dj-mix) can bundle it.
export const SITE_SEMVER = {
  major: ${Number(major)},
  minor: ${Number(minor)},
  patch: ${Number(patch)},
  prerelease: '${prerelease}',
  buildDate: '${buildDate}',
};

export function getVersion() {
  const { major, minor, patch, prerelease } = SITE_SEMVER;
  const base = \`${'${major}'}.${'${minor}'}.${'${patch}'}\`;
  return prerelease ? \`${'${base}'}-${'${prerelease}'}\` : base;
}

export function getBuildDate() {
  return SITE_SEMVER.buildDate || '';
}
`;
fs.writeFileSync(djMixVersionTarget, djMixVersionContent, 'utf8');
console.log(`Updated ${djMixVersionTarget} -> ${rawVersion}`);

// ── dj-mix/sw.js — CACHE version ──
const djMixSwTarget = path.resolve(process.cwd(), 'dj-mix/sw.js');
let djMixSwContent = fs.readFileSync(djMixSwTarget, 'utf8');
djMixSwContent = djMixSwContent.replace(
  /^const CACHE = 'djmix-v[^']+';$/m,
  `const CACHE = 'djmix-v${Number(major)}.${Number(minor)}.${Number(patch)}';`
);
fs.writeFileSync(djMixSwTarget, djMixSwContent, 'utf8');
console.log(`Updated dj-mix/sw.js CACHE -> djmix-v${rawVersion}`);

