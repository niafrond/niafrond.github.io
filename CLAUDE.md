# Instructions IA

- Toujours mettre à jour `dj-mix/SPECS.md` à chaque prompt pour refléter les changements effectués.
- Pour tout travail sur `dj-mix` ou `mix-blind-test` : toujours consulter `http://127.0.0.1:3000/swagger.json` en début de session pour connaître les endpoints disponibles du serveur local.
- Pour tout travail sur `dj-planner` : toujours consulter `http://127.0.0.1:8001/openapi.json` en début de session pour connaître les endpoints disponibles du serveur local.
